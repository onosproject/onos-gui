sudo: false

services:
  - docker

notifications:
  email: false

branches:
  only:
    - master

jobs:
  include:
    - stage: tests
      script: docker container run --rm -it onosproject/onos-gui-build build





