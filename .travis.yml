sudo: false

services:
  - docker

notifications:
  email: false

branches:
  only:
    - master

jobs:
  include:
    - stage: tests
      script: docker container run --rm -it -v $(pwd):/opt/web/onos-gui -w /opt teracy/angular-cli " npm init && cd .. && make"





